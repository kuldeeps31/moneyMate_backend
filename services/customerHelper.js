// helpers/customerHelper.js
//exports.getStatusAndNextPaymentText = function(paid, total, nextDate) {
//  const status = (total - paid) === 0 ? "paid" : "due";
//  const today = new Date();
//  const dueDate = new Date(nextDate);
//  const diffDays = Math.ceil((dueDate - today) / (1000 * 60 * 60 * 24));
//  const nextPaymentText = diffDays <= 0 ? "TODAY" : `in ${diffDays} days`;
//  return { status, nextPaymentText };
//};
